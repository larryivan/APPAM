Small APPAM-SMK demo input for UI and preflight smoke tests.

Use these project-relative values after copying this directory into a project workspace:

- `sample_manifest`: `examples/appam-smk/samples.tsv`
- `raw_data_dir`: `examples/appam-smk/raw`

The reads are intentionally tiny and are not suitable for biological interpretation.
